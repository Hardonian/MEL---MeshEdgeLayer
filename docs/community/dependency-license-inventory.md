# Dependency and license inventory

- Go stdlib runtime dependencies only for the main daemon and CLI.
- Meshtastic protobuf schema files are stored in-repo for compatibility parsing.
- SQLite is used through the system `sqlite3` CLI in this environment.
- Project license: MIT.
